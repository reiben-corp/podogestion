# Sesión 2026-09-15 — Clínica Podología

## Estado actual del proyecto

### Stack
- Backend: FastAPI + Python + SQLAlchemy + PostgreSQL (Docker Compose)
- Frontend: React + Vite (TypeScript)
- Auth: JWT local (sin OAuth2)
- DB: PostgreSQL

### Puertos
- Frontend: 5173 (Vite dev server)
- Backend: 8000 (FastAPI/Uvicorn)

### Credenciales
- Usuario: admin
- Password: admin123456

## Última sesión (15/09/2026)

### CSS revertido
- **`frontend/src/index.css`** restaurado al estado del commit `c9fe52f` (diseño definitivo de ficha del paciente).
- Cambios revertidos:
  - `.modal` volvió a `padding: 2rem` y `overflow-y: auto` (antes se había puesto `padding: 0` y `display: flex` para el header fijo)
  - `.ficha-modal-header` mantiene `border-bottom: 1px solid #e0e0e0`
  - `.form-seccion-titulo` sin `border-bottom` y `font-size: 1.1rem`
  - `.modal h2` sin `border-bottom`

### Módulos implementados
1. **Pacientes** — CRUD completo con ficha detallada (modal)
2. **Historia Clínica** — Formulario único de 10 secciones (FormHistoriaClinica.tsx)
3. **Citas** — CRUD con buscador de pacientes
4. **Facturación** — CRUD de documentos
5. **Inventario** — CRUD de productos

### Componentes clave
- `FormHistoriaClinica.tsx` — Formulario completo HC (nuevo, no existe en commit anterior)
- `FormPaciente.tsx` — Formulario alta/edición paciente
- `FormCita.tsx` — Formulario de citas
- `Podograma.tsx` — Componente de podograma interactivo
- `Modal` (en Dashboard.tsx) — Componente de modal reutilizable con ficha-modal-header

### Pendiente
- Módulo de Administración (auditoría de cambios)
- Integración con Supabase (futuro)
- Login con GitHub (pendiente OAuth)

## Lecciones aprendidas (ya en memory)
- Al crear formularios nuevos, revisar TODOS los campos del modelo SQLAlchemy primero
- Después de cambios en frontend, ejecutar `docker exec clinica_frontend npx tsc --noEmit`
- Los modales con `ficha-modal-header` necesitan `padding: 0` y estructura flex especial
- No hay node_modules local; usar docker exec para verificar TS
